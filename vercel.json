{
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "buildCommand": "npm run build",  // Or your custom command
  "outputDirectory": "dist"  // Or "build" if that's where your files go
}
